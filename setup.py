from setuptools import setup, find_packages

setup(
    name='STservo_sdk',
    version='0.1.0',
    description='Python SDK do sterowania serwami ST',
    author='ktoś',
    packages=find_packages(),  # automatycznie znajdzie STservo_sdk
    install_requires=[],
)
